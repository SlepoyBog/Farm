# 🏭 AI Content Farm — Полный отчёт

**Дата:** 2026-05-22  
**Статус:** Активна, оптимизирована  
**Платформы:** Telegram, VK, Яндекс.Дзен (кросс-постинг), SEO-сайт (GitHub Pages)

---

## 1. Архитектура

```
┌─────────────────────────────────────────────────┐
│              DeepSeek Flash API                  │
│  ($0.27/1M input · $1.10/1M output)             │
└──────────┬──────────┬──────────┬────────────────┘
           │          │          │
     ┌─────▼──┐ ┌────▼───┐ ┌───▼──────┐
     │ Статья │ │ TG     │ │ SEO      │
     │ готовая│ │ адапт  │ │ оптим.   │
     └───┬────┘ └───┬────┘ └───┬──────┘
         │          │          │
         ▼          ▼          ▼
   ┌─────────┐ ┌────────┐ ┌──────────┐
   │ VK пост  │ │Telegram│ │SEO-сайт  │
   │(адапт-я  │ │канал   │ │+ sitemap │
   │ на лету) │ │        │ │+ RSS     │
   └─────────┘ └────────┘ └──────────┘
         │          │            │
         │          ▼            │
         │    ┌──────────┐      │
         │    │Яндекс.Дзен│      │
         │    │(авто-     │      │
         │    │ кросс-пост│      │
         │    └──────────┘      │
         ▼                     ▼
   ┌──────────┐       ┌──────────────┐
   │GitHub     │       │GitHub Pages  │
   │(репозиторий)     │(сайт)        │
   └──────────┘       └──────────────┘
```

## 2. Структура проекта

```
Farm/
├── .env                          # Токены API, SITE_URL
├── .gitignore
├── requirements.txt              # Зависимости
├── FARM_REPORT.md                ← этот файл
│
├── src/
│   ├── main.py                   # Оркестратор: генерация, публикация
│   ├── deepseek_client.py        # Клиент DeepSeek Flash API
│   ├── trend_analyzer.py         # Анализ трендов (Google Trends + post_history + сезон)
│   ├── site_generator.py         # Генерация SEO-сайта (HTML, sitemap, RSS)
│   ├── scheduler.py              # Планировщик (--once для Task Scheduler)
│   ├── vk_publisher.py           # Публикация VK
│   ├── feedback_loop.py          # Сбор метрик + анализ топ-постов
│   ├── seo_optimizer.py          # SEO-оптимизация статьи
│   └── batch_worker.py           # Пакетная обработка очереди
│
├── prompts/
│   ├── generate_topics.prompter  # Промпт генерации тем ({{count}} штук)
│   ├── generate_outline.prompter # (не используется — заменён шаблонами)
│   ├── write_article.prompter    # Написание статьи
│   ├── critique_article.prompter # Критика статьи
│   ├── rewrite_article.prompter  # Рерайт по замечаниям
│   ├── tg_trend_editor.prompter  # Адаптация под Telegram
│   ├── vk_trend_editor.prompter  # (не используется — VK адаптирует сам)
│   ├── seo_optimizer.prompter    # SEO-оптимизация
│   └── trend_analysis.prompter   # Анализ трендов (резерв)
│
├── data/
│   ├── post_history.json         # История публикаций + метрики
│   ├── topics_queue.txt          # Очередь тем для публикации
│   ├── niche_rotation_index.txt  # Индекс сезонной ротации
│   ├── schedule_state.json       # Состояние планировщика
│   ├── outline_templates.json    # Шаблоны планов статей (11 ниш)
│   └── topics_pool.json          # Пул предгенерённых тем
│
├── output/                       # 143+ сгенерированных статей (HTML)
├── site/                         # 143 страницы SEO-сайта
├── logs/                         # Логи API и процессов
│
├── run.bat                       # Тестовый запуск (1 пост, с паузой)
├── run_prod.bat                  # Боевой запуск (15 постов, без пауз, для Task Scheduler)
└── deploy.bat                    # Пуш сайта в GitHub Pages
```

## 3. API-вызовы (на 1 запуск × ~15 тем)

### Было (до оптимизации): ~256 вызовов/день

| Шаг | Вызовов | Комментарий |
|-----|:-------:|-------------|
| Выбор ниши | 8 | DeepSeek каждый раз |
| Генерация 15 тем | 8 | DeepSeek каждый раз |
| План статьи (15 тем) | 120 | DeepSeek на каждую |
| Статья (15 тем) | 120 | DeepSeek |
| TG-адаптация (15 тем) | 0 | не было |
| VK-адаптация (15 тем) | 0 | не было |
| SEO (15 тем) | 0 | не было |
| **Всего/день** | **~256** | |

### Стало (текущая оптимизация): ~120 вызовов/день

| Шаг | Вызовов | Экономия |
|-----|:-------:|:--------:|
| Выбор ниши | **0** | post_history + сезон |
| Генерация тем | **~1/нед** | пул на 50 тем, refill раз в неделю |
| План статьи | **0** | локальные шаблоны (11 ниш × 3-4 шаблона) |
| Статья | 120 | DeepSeek |
| TG-адаптация | 120 | DeepSeek, но max_tokens 2500→1000 |
| VK-адаптация | **0** | VK сам конвертирует HTML |
| SEO | 15 | batch-обработка 1 раз/день |
| **Всего/день** | **~120** | |

## 4. Расходы

### Модель тарифов

DeepSeek Flash: Input $0.27/1M токенов, Output $1.10/1M токенов  
Средний вызов (статья): ~300 input + ~1500 output → ~$0.0017  
SEO (после оптимизации): ~800 input + ~100 output → ~$0.0003

### Тестовый режим (1 пост/запуск)

| Период | Расход |
|--------|:------:|
| 1 запуск (1 пост) | ~$0.003 |
| День (~10 тестовых запусков) | ~$0.03 |
| **Месяц** | **~$1-2** |

### Полный режим (15 постов/запуск, 8 запусков/день)

| Период | Расход |
|--------|:------:|
| 1 запуск (15 постов) | ~$0.05 |
| День (8 запусков) | ~$0.40 |
| SEO-батч (только новые статьи, ~15 шт) | ~$0.005 |
| **Месяц (30 дней)** | **~$5-7** |

> SEO больше не перезаписывает всю статью — только метаданные.  
> Экономия: ~85% на SEO-вызовах. Первоначальная оптимизация 135 статей обошлась в ~$0.32 (разово).  

## 5. Платформы и доход

### Telegram
- Канал: публикация с HTML-форматированием
- Адаптация под TG-стиль: короткие абзацы, эмодзи, вовлекающие вопросы
- Лимит: 4096 символов на сообщение
- Авто-кросс-постинг в **Яндекс.Дзен** (настраивается в админке Дзена)

### VK
- Адаптация на лету: HTML → текст + хештеги + вопрос
- Поддержка digest-постов (несколько тем в одном посте)
- API VK v5.199

### SEO-сайт (GitHub Pages)
- Адрес: `SITE_URL` из `.env` (сейчас https://your-niche-blog.pages.dev)
- Генерация: HTML-страницы + sitemap.xml + RSS + robots.txt
- Deploy: `deploy.bat` → git push → GitHub Actions → Pages
- SEO-теги: title, description, keywords, Open Graph, canonical
- Статей на сайте: **116**

## 6. Ключевые файлы конфигурации

### `.env`
```
DEEPSEEK_API_KEY=sk-...
TELEGRAM_BOT_TOKEN=...
TELEGRAM_CHAT_ID=-100...
VK_ACCESS_TOKEN=vk1.a...
VK_GROUP_ID=club...
SITE_URL=https://ваш-сайт.pages.dev
UNSPLASH_ACCESS_KEY=...
NEWSAPI_KEY=...              # опционально, для русских новостей (регистрация newsapi.org)
```

### `data/outline_templates.json`
11 ниш, 3-4 шаблона плана на каждую.  
Выбирается случайный шаблон, соответствующий теме (определяется по ключевым словам).

### `data/topics_pool.json`
Автоматически пополняется раз в 7 дней.  
Хранит до 50 тем на нишу. При каждом запуске берётся 15 тем из пула.

## 7. Запуск

| Команда | Режим | Описание |
|---------|-------|----------|
| `run.bat` | Тестовый | 1 пост, с паузой (ручная отладка) |
| `run_prod.bat` | Боевой | 15 постов + фидбек + деплой, без пауз (для Task Scheduler) |
| `deploy.bat` | Утилита | Пуш сайта в GitHub Pages |
| `python src/scheduler.py --flush` | Утилита | Обработать всю очередь немедленно |
| `python src/scheduler.py --status` | Утилита | Статус планировщика |

## 8. Feedback Loop

Собирает метрики из VK (просмотры, лайки, комментарии, репосты).  
Анализирует топ-3 поста за 30 дней.  
Генерирует рекомендации по стилю и структуре.  
Рекомендации впрыскиваются в system_prompt при генерации новых статей.

**Текущее состояние:** есть история 40+ постов, метрики VK собираются.  
Telegram-метрики не собираются (Bot API не отдаёт статистику каналов).

## 9. Известные ограничения

- Google Trends (`pytrends`) может не работать без авторизации — запасной вариант: post_history + сезонная ротация
- Telegram: метрики (просмотры/реакции) не собираются из-за ограничений Bot API
- GitHub Pages: нет серверной логики, только статика
- VK digest (комбинированный пост): собирается из базовых HTML-статей, без адаптации (может выглядеть неидеально)
- **VK фото**: `photos.getWallUploadServer` требует user-токен (не group). Сейчас фото не прикрепляются к постам VK до получения правильного токена.

## 10. Тестовый режим

Ферма запускается в **тестовом режиме** — 1 пост за запуск (вместо 15).  
Это позволяет отладить баги без лишних расходов и спама.

| Режим | Команда | Постов |
|-------|---------|:------:|
| Тестовый (по умолчанию) | `python src/main.py` | 1 |
| Полный | `python src/main.py --full` / `run_prod.bat` | 15 |

Дополнительно:
- Записи о публикациях всё равно попадают в `post_history`
- Когда ферма будет полностью отлажена, тестовый режим отключается

## 11. GitHub Actions (удалённый запуск)

### Воркфлоу

Файл: `.github/workflows/publish.yml`

| Триггер | Расписание (MSK) |
|---------|:----------------:|
| `schedule` | 09:00, 13:00, 17:00, 21:00 |
| `workflow_dispatch` | ручной запуск из UI GitHub |

### Secrets (настроить в Settings → Secrets and variables → Actions)

| Secret | Описание |
|--------|----------|
| `DEEPSEEK_API_KEY` | Ключ DeepSeek API |
| `TELEGRAM_BOT_TOKEN` | Токен Telegram-бота |
| `TELEGRAM_CHAT_ID` | ID канала |
| `VK_ACCESS_TOKEN` | Токен VK API |
| `VK_GROUP_ID` | ID группы VK |
| `SITE_URL` | URL сайта |
| `UNSPLASH_ACCESS_KEY` | API-ключ Unsplash (бесплатный, регистрация на unsplash.com) |
| `NEWSAPI_KEY` | API-ключ NewsAPI.org (опционально, 100 запр/день бесплатно) |

### Процесс

1. GitHub Actions запускает `python src/main.py` (1 пост, тестовый режим)
2. Запускает `python src/feedback_loop.py` (сбор метрик)
3. Коммитит и пушит `site/` обратно в репозиторий (с `[skip ci]` для предотвращения цикла)

Переменные окружения передаются через `${{ secrets.* }}` — `load_dotenv()` в коде не перезаписывает уже установленные env-переменные, поэтому изменений в коде не требуется.

---

## 12. История оптимизаций

| Дата | Изменение | Экономия/мес |
|------|-----------|:------------:|
| 2026-05-22 | Исправлен batch_worker (NICHE import error) | — |
| 2026-05-22 | Подключён SEO-оптимизатор | качество↑ |
| 2026-05-22 | SITE_URL вынесен в .env | — |
| 2026-05-22 | Удалён мёртвый image_generator.pyc | — |
| 2026-05-22 | Заголовки статей очищаются от эмодзи | качество↑ |
| 2026-05-22 | Убрана двойная VK-публикация | ~$2 |
| 2026-05-22 | Удалён enhance_for_vk | ~$6 |
| 2026-05-22 | SEO перенесён в batch (1 раз/день) | ~$8 |
| 2026-05-22 | Температура TG: 0.8→0.4, токены: 2500→2000 | ~$2 |
| 2026-05-22 | Outline-шаблоны вместо API (11 ниш) | ~$6 |
| 2026-05-22 | Topic pool (50 тем/нед, refill раз в 7 дней) | ~$0.50 |
| 2026-05-22 | Выбор ниши через post_history (0 API) | ~$0.50 |
| 2026-05-22 | Temperature TG: 0.8→0.4, токены 2000→1000 | +~$2 |
| 2026-05-22 | **Исправлен баг**: лишние `\` в Telegram (Markdown-экранирование в HTML-режиме) | — |
| 2026-05-22 | **Тестовый режим**: 1 пост/запуск, флаг `--full` для полного цикла | ~$25 |
| 2026-05-22 | **Батники**: 5→2 (`run.bat` + `run_prod.bat`), удалены старые | — |
| 2026-05-22 | **SEO оптимизирован**: только метаданные, без перезаписи статьи (max_tokens 3000→500) | ~$8 |
| 2026-05-22 | **TG лимит увеличен**: max_tokens 1000→2000 (ответ обрезался, пост был пустой) | +~$4 |
| 2026-05-22 | **Исправлен баг**: дублирование заголовка в VK (h1 не удалялся из тела поста) | — |
| 2026-05-22 | **GitHub Actions**: 4 запуска/день, автоматический деплой, без участия пользователя | — |
| 2026-05-22 | **Исправлен TG**: очистка markdown-блоков (\`\`\`html) и лишних пробелов | качество↑ |
| 2026-05-22 | **Workflow**: тестовый режим (1 пост) в GitHub Actions | — |
| 2026-05-22 | **GitHub Actions**: исправлена ошибка Pages (Pages → GitHub Actions) | — |
| 2026-05-22 | **GitHub Actions**: удалён дублирующий deploy.yml | — |
| 2026-05-22 | **GitHub Actions**: создание `logs/` и `output/` на раннере | — |
| 2026-05-22 | **GitHub Actions**: добавлен пропущенный `import os` в deepseek_client.py | — |
| 2026-05-22 | **GitHub Actions**: DEEPSEEK_API_KEY для feedback_loop | — |
| 2026-05-24 | **Подключён Unsplash**: картинки к постам через Unsplash API | — |
| 2026-05-24 | **Исправлен баг**: дублирование постов в TG (sendPhoto + sendMessage → 1 сообщение с URL картинки и превью) | — |
| 2026-05-24 | **Исправлен баг**: дублирование картинок (удалён `_cache[niche]` в `image_provider.py`) | — |
| 2026-05-24 | **VK**: добавлено детальное логирование `_upload_wall_photo` (пошагово + типы исключений) | — |
| 2026-05-24 | **VK**: обнаружена ошибка 27 (group auth) — `photos.getWallUploadServer` не работает с group-токеном | — |
| 2026-05-25 | **Исправлен баг**: URL Unsplash отображался текстом перед постом в Telegram. Вместо `sendMessage` с URL используется `sendPhoto` — изображение встраивается напрямую, без видимой ссылки | — |
| 2026-05-25 | **Исправлен выбор ниши**: `_from_post_history()` возвращал «технологии» всегда (все score=null → max() брал первый ключ). Теперь требует `score != null`. DeepSeek-запрос ограничен списком из 10预设ленных ниш с маппингом. Fallback циклически проходит по всем 10 нишам вместо 4 сезонных. | качество↑ |
| 2026-05-25 | **RSS + DeepSeek для трендов**: новый модуль `trend_reader.py` — парсит 9 RSS-лент Яндекс.Новостей. Заголовки передаются DeepSeek как контекст для выбора hottest-ниши в реальном времени. | качество↑ |
| 2026-05-25 | **Исправлен баг**: TG-текст не влезал в 1 пост из-за лимита caption 1024 в sendPhoto. Уменьшен объём TG-адаптации: 2000-3000 → 700-900 символов, max_tokens 2000 → 500. | качество↑ |
| 2026-05-25 | **Замена источников трендов**: Яндекс.Новости (недоступен с US IP GitHub Actions) → NewsAPI.org (русские новости, опционально) + Reddit r/all (мировые тренды, без ключа) + BBC/CNN RSS (fallback). Google Trends удалён из цепочки (стабильно 404). | надёжность↑ |
| **Итого** | | **~$0 (только GitHub Actions)** |
